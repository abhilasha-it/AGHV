# research

## Structure

- `src/` — source code
- `data/` — input datasets
- `results/` — generated outputs, figures, results
- `notebooks/` — exploratory notebooks

## Setup

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```
